# July 19, 2026

## Phone game-chrome and map clarity

Hands-on phone review exposed a mismatch between the amount of interface chrome and the space needed
for the route atlas. At the 32rem vertical-map breakpoint, the active HUD now keeps only Time, Moves,
the complete wrapping Target, and an available Back action. Score, the separate round identity, the
map prompt row, and the floating zoom/navigation toolbar no longer compete with the first node. The
horizontal hint dock is one 44px row of icons and numeric penalties; its complete names, costs,
availability, and state remain in accessible labels.

The initial phone camera uses the same upper-stage anchor as subsequent moves, placing **You are
here** close beneath the HUD without hiding the complete frontier. A hidden toolbar can no longer
contribute a stale safe-area offset when the selected-route detail tray is revealed. After a move,
the existing full-frontier fitting still keeps Back, current, and every new choice reachable.

Phone choice pins are reduced from 44px to 40px while their surrounding controls remain at least
44px. Decorative WebGL and SVG tokens receive the matching 90% phone-only scale; desktop geometry is
unchanged. The distant target card no longer inherits the artwork column when the compact artwork is
absent, so long targets wrap completely and remain horizontally centered. Row-based choice labels
continue to use the tallest measured label in each row and vertically center shorter neighbors.

## Complete mobile node labels

The same no-truncation rule now covers the entire phone map, not only the active frontier. History,
discarded, current, distant-goal, and selected route-preview labels use natural wrapping with no
ellipsis. One shared line observer feeds every map label into the mobile projection. Cards that grow
above a marker reserve incoming clearance, active-choice rows reserve outgoing clearance, and a
long distant goal expands the world before its bottom margin. Constellation peers share the tallest
measured line count so a short name remains vertically centered beside a four-line name.

The regression uses the longest published atlas label, a 121-character entity name, at 320×568 and
390×844. It asserts the complete text, normal wrapping, clipped text-overflow mode rather than an
ellipsis, full element height, and no intersection with history, current, goal, or frontier peers.
Canonical Compose acceptance at `http://localhost` replayed an Alan Turing → Royal Society move at
390×844 and kept complete multi-line history and six-choice frontier labels separated. The video
heartbeat capture was regenerated from the corrected interface so the submission edit no longer
contains baked-in ellipses.

## Reachable-target heartbeat

The reachable target was too easy to mistake for an ordinary route. Its phone pin and desktop goal
card now share a continuous 1.05-second two-beat size heartbeat. Phone peaks are intentionally
stronger because the marker is small; the larger desktop card uses the same rhythm with restrained
scale. Both the system preference and Webwoven's in-app reduced-motion setting stop the animation
while preserving the static Ochre goal treatment.

Focused browser coverage locks the unclipped phone HUD, hidden mobile toolbar and prompt row, compact
hint controls, 40px visible pin with a larger touch target, centered multi-line goal, initial camera
anchor, complete post-move frontier, and shared mobile/desktop heartbeat name. The changes were
reviewed interactively from a physical phone over the LAN Vite hot-reload surface before release.

## Desktop opening-stage containment

A production follow-up exposed that the opening desktop fit included the start and active choices
but omitted a distant goal. Closing the endpoint introduction then applied ordinary stage-follow
panning and could move that goal partly or completely outside the clipped map viewport. The initial
desktop bounds now include the distant goal, and the transition from introduction to active play
refits the opening stage instead of treating it as a completed move. A 1341×868 browser regression
measures both complete endpoint cards inside the real map viewport after the introduction closes.

## Mobile dead-end Back containment

Visual acceptance of the all-terminal recovery exposed that its intentionally stationary Back
camera could replace a compact history marker with the taller current card partly above the clipped
phone viewport. Vertical dead-end recovery now preserves the current zoom and continuity while
nudging the recovered current card and active choices just far enough inside the map. Desktop keeps
the existing stationary collapse. The exhausted-branch browser flow measures the complete recovered
current card against the real mobile map viewport and retains the unchanged-camera assertion on
desktop. Canonical Compose replayed `bird → Ornithurae → Back` at 390×844 and left the complete
returned `bird` card 17px below the map's clipped top edge while both choices and the goal remained
visible.

## All-terminal frontier recovery

Production lobby `90PSJ6` exposed a route state where `Ornithurae` offered `Ornithuromorpha` and
`evolution of birds`, although both destinations had no route-safe edge except the blocked return to
`Ornithurae`. The existing safeguard only examined a frontier with exactly one destination, so two
equivalent terminal choices bypassed it. Frontier projection now follows the forced continuation
through one shared route-aware search and treats the current node as exhausted when no path can reach
the target without revisiting the active route. A dead-end decoy remains playable when at least one
real alternative exists, and that safe first move is guaranteed to survive the six-choice display
cap. Compass and frontier projection now share the same reachability owner. The fix is derived from
the pinned graph and active route; no entity blacklist or graph-data edit is involved.
A full current-bundle sweep followed the retained safe move through all 100 published rounds and
reached every target across 301 frontier projections. On the local SQLite adapter, projection took
4.05ms on average, 18.62ms at the 95th percentile, and 34.24ms at maximum.

Canonical Compose acceptance rebuilt the API against graph build
`bcef5c35ef4725305ba3e71124f0925383fdebd26e0c70d33c9af9f0253b62d1` and replayed the exact
`round-b99863c778102356` state through the real HTTP session contract. Following `bird` to
`Ornithurae` produced zero visible targets while retaining `bird` in the navigation stack, so the
client presents the existing Back recovery instead of either false continuation.

## Release validation

The complete quality gate passes 164 web tests, 363 Python tests, both Remotion composition checks,
formatting, lint, Svelte and Python type checks, file-size and color-policy checks, and the strict
documentation build. All 48 Playwright flows pass across desktop and mobile, including both
reduced-motion paths for the reachable-goal heartbeat. Layout-focused browser coverage was split
into its own responsibility-owned spec so the modified test files remain below 600 lines.

Canonical Compose acceptance rebuilt Caddy at `http://localhost` against the active real Wikidata
bundle. A fresh 390×844 galaxy → Sun round placed the current card 61px below the map viewport edge,
kept the 40px choice pins inside their larger controls, wrapped the Target in full, hid Score, the map
header, and the floating toolbar, and produced no horizontal document overflow. At 1341×868 the same
compiled bundle returned to the horizontal card layout with Score, map header, and utility rail
visible. The emitted production CSS contains the shared `reachable-goal-heartbeat` animation, and
the local UI and readiness endpoints both return HTTP 200.

All five jobs in the
[GitHub quality run](https://github.com/Hyperrick/webwoven/actions/runs/29678319552) passed for
gameplay release `3639d35`, including the 44-flow browser matrix and container build. The immutable
production release now points to that commit. Public
`/health/live`, `/health/ready`, and `/health/graph` checks are healthy against graph build
`bcef5c35ef4725305ba3e71124f0925383fdebd26e0c70d33c9af9f0253b62d1`.

Final public browser acceptance replayed `round-b99863c778102356` through the real production API:
`bird → Ornithurae` returned no false choices and kept Back available, then the returned phone card
remained fully below the HUD. A fresh desktop round kept both opening endpoint cards inside the map
viewport. The production browser reported no console errors or warnings.

## Submission-ready repository snapshot

The public repository landing page, documentation home, production system map, delivery gate, API
notes, and copy-ready Devpost story now describe the complete release rather than the Build Week
midpoint. The final story includes all 100 published start/goal round definitions, Multiplayer Lobby sharing and
same-code rematches, the compact mobile interface and complete labels, the reachable-target
heartbeat, completion confetti, and route-aware all-terminal recovery. Current validation numbers
are synchronized at 164 web tests, 363 Python tests, and 48 desktop/mobile Playwright flows.

The saved Devpost draft already contains a product-map thumbnail and two product gallery images.
The repository versions of the desktop map and phone gallery were refreshed from the final July 19
interface and must still be synchronized to the draft after owner approval; the synthetic Messi
artwork remains YouTube-only. Repository publication, rights and terms acceptance, and the final
Devpost submit action remain separate owner decisions.

## Replacement submission video V2

The earlier YouTube upload was deleted after the final product review exposed stale Relay, Par, and
40-route visuals. There is no public demo URL until the owner uploads the verified replacement.
The V2 edit uses current Multiplayer and Lobby language, keeps Target visible without Par, shows the
compact phone interface, and reports all 100 checked start/goal rounds.

The replacement source narration is 190.928938 seconds long. Remotion is configured to play it at
1.09× for an effective duration of approximately 175.164 seconds inside a 178-second, 5,340-frame
composition at 30 fps. Narration begins after a one-second composition delay. Its corrected sidecar
contains 40 retimed cues and ends at 176.138 seconds.
The eight planned scene ranges are Hook 0:00.0–0:12.0, Start 0:12.0–0:28.7, Explain
0:28.7–0:55.4, Modes 0:55.4–1:15.9, Codex 1:15.9–1:46.8, Iteration 1:46.8–2:13.5, Trust
2:13.5–2:43.8, and Close 2:43.8–2:58.0.

The final narrated render also includes the supplied Gleb D track from Pixabay at a subtle 0.055
volume beneath the voiceover. The reusable Remotion track loops through the full edit, fades in over
two seconds, and fades out over the final three seconds. The exact creator and Pixabay links are
recorded in `THIRD_PARTY_NOTICES.md` and must be included in the public YouTube description.

The corrected owner-review file `output/webwoven-build-week-demo-v2-preview.mp4` was rendered at
960×540, 30 fps, with H.264 video and 48 kHz AAC stereo audio. A voice-only timing check measured
1.126 seconds before the narration becomes audible. The 40-cue sidecar ends with the spoken
narration at 2:56.138. The full-resolution delivery remains blocked on owner approval.

Owner review found that the Explain heading inherited the generic 880px title limit and intersected
the product browser by approximately nine pixels. The scene now reserves separate grid rows for the
heading and browser, gives the fixed heading enough width to remain on one line, and keeps a 32px
gap between them. The composition also fades from full black over its first 18 frames and returns to
full black over its final 18 frames without changing music or total duration. The Hook browser chrome
is darkened so no light strip remains above its dark opening panel. Targeted frame checks confirmed
the first frame, full reveal, and the corrected top edge. The low-resolution review SHA-256 is
`bc0f275c61bd716d25c029708b10c77ac1ad59d6ea2408855db08ed04a16bf84`.

The next owner-review pass set the Start headline explicitly as “Choose the / kind of path / you
want” and applied the same dark browser-chrome treatment to the Trust scene at 2:14, removing its
top-edge light flash. A deliberately compressed 480×270 approval render is available at
`output/webwoven-build-week-demo-v2-rough-preview.mp4`; its SHA-256 is
`1d1e076b5c8a3da8d97003056f6e153da75a62756096d09992b6402861f5ba8b`. The same browser-chrome fix
also covers the complete Close scene from 2:44 through the final fade.

After owner approval, the final 1920×1080 delivery was rendered at 30 fps with H.264 video and
48 kHz AAC stereo audio. The 2:58 file measures −15.4 LUFS with a −0.7 dBFS true peak and has
SHA-256 `cc8db746a36fcc3d11554d74dd41c13c4e759c69214f9d28cdad5770350437b0`. Encoded-frame checks
confirmed the opening, the corrected Start headline, the dark top edge at 2:14 and 2:44, and the
final fade to black.

Three new Devpost gallery screenshots were then captured natively from the real Compose product at
an exact 1620×1080 desktop viewport. The browser session started a fresh Desmond Tutu to Eleanor
Roosevelt route, captured its active frontier, and completed it through Nobel Peace Prize and United
Nations before capturing the result. No pre-existing video still or image asset was reused.

The final browser gate exposed a timing race in the reduced-motion Hard-intro assertion: under load,
the five-second intro could finish before the test inspected its transient `inert` state. The test
now controls the browser clock, verifies the locked intro and endpoint artwork, advances past the
handoff, and verifies the unlocked game. The complete serial Playwright gate then passed all 48
desktop and mobile flows.
